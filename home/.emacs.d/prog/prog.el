(require 'config-utils)

(load-configurations
 '(general
   c
   clojure
   coffee     ; CoffeScript
   clisp      ; Common Lisp
   elisp
   haskell
   java
   nasm       ; (n)ASM
   purescript
   python
   ruby       ; Ruby, Rails && Co.
   scala
   scss
   scheme     ; Scheme && Racket
   web-mode))
